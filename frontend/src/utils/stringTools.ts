

const StringTools = (str: string) => {
    
}

export const capitalizeFirstCharacter = (str: String): String => {
    return str.charAt(0).toUpperCase() + str.slice(1);
}

export default StringTools;
